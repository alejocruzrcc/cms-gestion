var vHeight = 430;
var vWidth = 600;
var pHeight = 600;
var pWidht = 400;

$(document).ready(function () {
    $('#video').css({
        height: vHeight + 'px',
        width: vWidth + 'px',

    });
    $('#the-canvas').css({
        width: pWidht + 'px',
        height: pHeight + 'px'
    });


});
